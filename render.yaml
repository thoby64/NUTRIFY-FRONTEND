services:
  - type: web
    name: nutrition-analytic-app
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python3 serve.py $PORT
    envVars:
      - key: PYTHONUNBUFFERED
        value: 1
    autoDeploy: true
